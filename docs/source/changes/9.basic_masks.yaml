category: added
summary: "Add mask for wires, layers, and parameters"
description: |
  This change adds basic functionality to mask the dimensions of wires, layers, and 
  single parameters.
pull requests:
  - 9
